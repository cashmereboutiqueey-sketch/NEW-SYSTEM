node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env*.local
*.log
npm-debug.log*
.DS_Store
next-env.d.ts
coverage/
src/generated/
tsconfig.tsbuildinfo

# Production build output, kept apart from the dev server (see next.config.ts)
.next-build/

# Verified database backups
backups/

# Uploaded product photographs (backed up with the database, not in git)
data/uploads/

# The SKU plan and its rollback: generated against a live Shopify catalogue,
# and the rollback names real variant ids. Regenerate rather than commit.
shopify-sku-plan.csv
shopify-sku-rollback.json

# Production settings live on the server, never here.
.env.production
Caddyfile.local
