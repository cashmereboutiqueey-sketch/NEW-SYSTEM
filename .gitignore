node_modules/
.next/
out/
build/
dist/
.env
.env.local
.env*.local
*.log
npm-debug.log*
.DS_Store
next-env.d.ts
coverage/
src/generated/
tsconfig.tsbuildinfo

# Production build output, kept apart from the dev server (see next.config.ts)
.next-build/

# Verified database backups
backups/

# Uploaded product photographs (backed up with the database, not in git)
data/uploads/
